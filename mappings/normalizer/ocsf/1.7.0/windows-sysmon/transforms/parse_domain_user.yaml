schema_version: "pa.transform.v0.1"
id: "parse_domain_user"
type: "split_domain_user"
params:
  delimiter: "\\"
  absent_sentinels: ["", "-", "(null)"]
  # Output contract:
  # - If input contains delimiter and both parts are non-empty:
  #     { "domain": "<left>", "name": "<right>" }
  # - Else if input non-empty:
  #     { "name": "<input>" }
  # - Else: absent

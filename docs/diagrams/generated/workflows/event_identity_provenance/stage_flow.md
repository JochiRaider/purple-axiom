# Stage flow — Event identity and provenance (deterministic join keys) (event_identity_provenance)

```mermaid
flowchart LR
  subgraph ci_environment["CI Environment (pipeline stages)"]
    normalization["Normalization Stage"]
  end
```

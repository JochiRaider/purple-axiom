# Stage flow — Provisioning and environment bring-up (provisioning)

```mermaid
flowchart LR
  subgraph ci_environment["CI Environment (pipeline stages)"]
    lab_provider["Lab Provider Stage"]
  end
```

# Stage flow — Criteria pack resolution and snapshot (validation stage internals) (criteria_pack_resolution)

```mermaid
flowchart LR
  subgraph ci_environment["CI Environment (pipeline stages)"]
    validation["Validation Stage"]
  end
```
